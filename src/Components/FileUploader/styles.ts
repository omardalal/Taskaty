import { CSSProperties } from "react";

export const styles = {
  attachmentsContainer: {
    display: "flex",
    flexDirection: "column",
    maxHeight: 225,
    overflow: "auto"
  } as CSSProperties,
  title: {
    fontSize: "0.75rem",
    color: "#525252",
    fontWeight: 400,
    marginBottom: 8
  } as CSSProperties
};
