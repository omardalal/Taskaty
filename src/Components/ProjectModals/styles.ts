import { CSSProperties } from "react";

export const styles = {
  formContainer: {
    width: "100vw",
    maxWidth: 500,
    margin: "auto"
  } as CSSProperties,
  modal: {
    border: "none",
    backgroundColor: "rgba(0 0 0 / 0.8)",
    margin: 0,
    padding: 0,
    display: "flex",
    width: "100%",
    left: 0,
    top: 0,
    height: "100%",
    borderRadius: 0
  } as CSSProperties
};
